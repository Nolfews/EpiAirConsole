# Backend environment example (keep a local `.env` on your machine)
# bind host for the server (0.0.0.0 to allow LAN access)
HOST=0.0.0.0

# port used by the backend
PORT=3000

# optional public URL you might get from ngrok or a domain. If set, the mobile client
# will use this as the default Server URL. 
# COMMENT OUT OR LEAVE EMPTY to enable auto-detection of all available URLs
# SERVER_URL=https://your-ngrok-url.ngrok-free.dev

# optional default room name for the mobile client
DEFAULT_ROOM=test-room
# Example environment variables for local development
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=your_db_name
